# Elevvo Backend Internship – Task 9

**Dockerizing the Backend Application**

## Description

The complete Product API (Prisma + Express + Swagger) packaged inside a Docker container.

## Features

- Dockerfile for the application
- docker-compose support
- Prisma Client generation inside container
- Clean .dockerignore

## How to Run with Docker

### Build the image
```bash
docker build -t elevvo-api .